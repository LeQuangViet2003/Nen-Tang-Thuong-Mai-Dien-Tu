{
  "name": "ecommerce-simplified",
  "version": "1.0.0",
  "description": "Nền tảng Thương mại điện tử phiên bản thu gọn",
  "scripts": {
    "setup": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "seed": "cd backend && npx prisma db push && node routes/seed.js",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
